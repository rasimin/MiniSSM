<Window x:Class="SSMS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="MiniSSMS - Desktop SQL Client" 
        Height="760" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#181818"
        Foreground="#D4D4D4"
        FontFamily="Segoe UI, Arial"
        Loaded="Window_Loaded"
        KeyDown="Window_KeyDown">

    <Window.Resources>


        <!-- Dark Theme Styles -->
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="MinWidth" Value="30"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="3" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2D2D30"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#3E3E42"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3E3E42"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#505050"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ExecuteButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Foreground" Value="#4EC9B0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="3" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1A3831"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4EC9B0"/>
                                <Setter Property="Foreground" Value="#4EC9B0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#244E44"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4EC9B0"/>
                                <Setter Property="Foreground" Value="#4EC9B0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dropdown ComboBox Dark Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#F1F1F1"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          Background="{TemplateBinding Background}" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          BorderThickness="{TemplateBinding BorderThickness}" 
                                          Foreground="{TemplateBinding Foreground}"
                                          Focusable="false"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="3">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="1" Text="▼" FontSize="8" Foreground="#888888" VerticalAlignment="Center" Margin="5,0,8,0"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="8,3,25,3"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Name="DropDownBorder" Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1" CornerRadius="3" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#F1F1F1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0E639C"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TreeView">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#D4D4D4"/>
            <Setter Property="BorderBrush" Value="#2D2D30"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeView">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ItemsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource StandardScrollBar}"/>
            </Style.Resources>
        </Style>

        <Style TargetType="TreeViewItem">
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="#CCCCCC" Margin="3,2" FontSize="12.5"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <!-- Remove bright Windows blue highlight, use a subtle dark color -->
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#2A2D2E"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFF"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#252526"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#CCCCCC"/>
            </Style.Resources>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#2D2D30"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <Style x:Key="QueryTabHeaderHorizontalScrollBar" TargetType="ScrollBar">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="6"/>
            <Setter Property="MinHeight" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Height="6" Background="#252526" SnapsToDevicePixels="True">
                            <Track x:Name="PART_Track"
                                   Orientation="{TemplateBinding Orientation}"
                                   IsDirectionReversed="False">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Opacity="0" Focusable="False" IsTabStop="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Opacity="0" Focusable="False" IsTabStop="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb MinWidth="24" Height="6">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border x:Name="ThumbBorder"
                                                        Height="6"
                                                        Background="#555555"
                                                        CornerRadius="3"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ThumbBorder" Property="Background" Value="#777777"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="QueryTabHeaderScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="6"/>
                            </Grid.RowDefinitions>

                            <ScrollContentPresenter Grid.Row="0"
                                                    Content="{TemplateBinding Content}"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                    CanContentScroll="{TemplateBinding CanContentScroll}"/>

                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Grid.Row="1"
                                       Orientation="Horizontal"
                                       Height="6"
                                       Style="{StaticResource QueryTabHeaderHorizontalScrollBar}"
                                       Value="{TemplateBinding HorizontalOffset}"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="QueryTabsControl" TargetType="TabControl">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#2D2D30"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <ScrollViewer Grid.Row="0"
                                          Background="#252526"
                                          HorizontalScrollBarVisibility="Auto"
                                          VerticalScrollBarVisibility="Disabled"
                                          CanContentScroll="False"
                                          Focusable="False"
                                          Style="{StaticResource QueryTabHeaderScrollViewer}">
                                <TabPanel x:Name="HeaderPanel"
                                          IsItemsHost="True"
                                          KeyboardNavigation.TabIndex="1"
                                          Panel.ZIndex="1"/>
                            </ScrollViewer>

                            <Border Grid.Row="1"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  ContentSource="SelectedContent"
                                                  Margin="{TemplateBinding Padding}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="#2D2D30" BorderThickness="0,0,1,1" Padding="12,6">
                            <ContentPresenter x:Name="ContentSite"
                                               ContentSource="Header"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,2,1,0"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="#969696"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Toolbar -->
            <RowDefinition Height="*"/>    <!-- Main Content -->
            <RowDefinition Height="25"/>   <!-- Status Bar -->
        </Grid.RowDefinitions>

        <!-- 1. Toolbar -->
        <Border Grid.Row="0" Background="#1E1E1E" BorderBrush="#2D2D30" BorderThickness="0,0,0,1" Padding="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                               <StackPanel x:Name="ToolbarPanel" Grid.Column="0" Orientation="Horizontal">
                    <Border Name="ToolbarConnect" Background="Transparent" Padding="2,0" Margin="0,0,4,0"
                            PreviewMouseLeftButtonDown="ToolbarItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="ToolbarItem_PreviewMouseMove"
                            PreviewMouseLeftButtonUp="ToolbarItem_PreviewMouseLeftButtonUp">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⋮" Foreground="#555555" VerticalAlignment="Center" Margin="2,0,4,0" Cursor="SizeAll" FontSize="14" FontWeight="Bold"/>
                            <Button Style="{StaticResource ToolbarButton}" Content="🔌" Click="BtnConnect_Click" ToolTip="Connect"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Name="ToolbarDisconnect" Background="Transparent" Padding="2,0" Margin="0,0,4,0"
                            PreviewMouseLeftButtonDown="ToolbarItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="ToolbarItem_PreviewMouseMove"
                            PreviewMouseLeftButtonUp="ToolbarItem_PreviewMouseLeftButtonUp">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⋮" Foreground="#555555" VerticalAlignment="Center" Margin="2,0,4,0" Cursor="SizeAll" FontSize="14" FontWeight="Bold"/>
                            <Button Style="{StaticResource ToolbarButton}" Content="✕" Click="BtnDisconnect_Click" ToolTip="Disconnect"/>
                        </StackPanel>
                    </Border>

                    <Border Name="ToolbarObjectExplorer" Background="Transparent" Padding="2,0" Margin="0,0,4,0"
                            PreviewMouseLeftButtonDown="ToolbarItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="ToolbarItem_PreviewMouseMove"
                            PreviewMouseLeftButtonUp="ToolbarItem_PreviewMouseLeftButtonUp">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⋮" Foreground="#555555" VerticalAlignment="Center" Margin="2,0,4,0" Cursor="SizeAll" FontSize="14" FontWeight="Bold"/>
                            <Button x:Name="BtnToggleObjectExplorer" Style="{StaticResource ToolbarButton}" Content="☰" Click="BtnToggleObjectExplorer_Click" ToolTip="Hide Object Explorer (F8)"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Name="ToolbarExecute" Background="Transparent" Padding="2,0" Margin="0,0,4,0"
                            PreviewMouseLeftButtonDown="ToolbarItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="ToolbarItem_PreviewMouseMove"
                            PreviewMouseLeftButtonUp="ToolbarItem_PreviewMouseLeftButtonUp">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⋮" Foreground="#555555" VerticalAlignment="Center" Margin="2,0,4,0" Cursor="SizeAll" FontSize="14" FontWeight="Bold"/>
                            <Button Style="{StaticResource ExecuteButton}" Content="▶" Click="BtnExecute_Click" Margin="5,0" ToolTip="Execute Query (F5)"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Name="ToolbarDatabase" Background="Transparent" Padding="2,0" Margin="0,0,4,0"
                            PreviewMouseLeftButtonDown="ToolbarItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="ToolbarItem_PreviewMouseMove"
                            PreviewMouseLeftButtonUp="ToolbarItem_PreviewMouseLeftButtonUp">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⋮" Foreground="#555555" VerticalAlignment="Center" Margin="2,0,4,0" Cursor="SizeAll" FontSize="14" FontWeight="Bold"/>
                            <Label Content="Database:" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="5,0" FontSize="12"/>
                            <ComboBox x:Name="CboDatabases" Width="200" VerticalAlignment="Center" SelectionChanged="CboDatabases_SelectionChanged"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Name="ToolbarSave" Background="Transparent" Padding="2,0" Margin="0,0,4,0"
                            PreviewMouseLeftButtonDown="ToolbarItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="ToolbarItem_PreviewMouseMove"
                            PreviewMouseLeftButtonUp="ToolbarItem_PreviewMouseLeftButtonUp">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⋮" Foreground="#555555" VerticalAlignment="Center" Margin="2,0,4,0" Cursor="SizeAll" FontSize="14" FontWeight="Bold"/>
                            <Button Style="{StaticResource ToolbarButton}" Content="💾" Click="BtnSaveQuery_Click" ToolTip="Save Query File (Ctrl+S)"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Name="ToolbarOpen" Background="Transparent" Padding="2,0" Margin="0,0,4,0"
                            PreviewMouseLeftButtonDown="ToolbarItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="ToolbarItem_PreviewMouseMove"
                            PreviewMouseLeftButtonUp="ToolbarItem_PreviewMouseLeftButtonUp">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⋮" Foreground="#555555" VerticalAlignment="Center" Margin="2,0,4,0" Cursor="SizeAll" FontSize="14" FontWeight="Bold"/>
                            <Button Style="{StaticResource ToolbarButton}" Content="📂" Click="BtnOpenQuery_Click" ToolTip="Open SQL File (Ctrl+O)"/>
                        </StackPanel>
                    </Border>

                    <Border Name="ToolbarComment" Background="Transparent" Padding="2,0" Margin="0,0,4,0"
                            PreviewMouseLeftButtonDown="ToolbarItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="ToolbarItem_PreviewMouseMove"
                            PreviewMouseLeftButtonUp="ToolbarItem_PreviewMouseLeftButtonUp">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⋮" Foreground="#555555" VerticalAlignment="Center" Margin="2,0,4,0" Cursor="SizeAll" FontSize="14" FontWeight="Bold"/>
                            <Button Style="{StaticResource ToolbarButton}" Content="--" Click="BtnCommentQuery_Click" ToolTip="Comment Selection (Ctrl+K)"/>
                        </StackPanel>
                    </Border>

                    <Border Name="ToolbarUncomment" Background="Transparent" Padding="2,0" Margin="0,0,4,0"
                            PreviewMouseLeftButtonDown="ToolbarItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="ToolbarItem_PreviewMouseMove"
                            PreviewMouseLeftButtonUp="ToolbarItem_PreviewMouseLeftButtonUp">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⋮" Foreground="#555555" VerticalAlignment="Center" Margin="2,0,4,0" Cursor="SizeAll" FontSize="14" FontWeight="Bold"/>
                            <Button Style="{StaticResource ToolbarButton}" Content="--×" Click="BtnUncommentQuery_Click" ToolTip="Uncomment Selection (Ctrl+Shift+K)"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Name="ToolbarNewQuery" Background="Transparent" Padding="2,0" Margin="0,0,4,0"
                            PreviewMouseLeftButtonDown="ToolbarItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="ToolbarItem_PreviewMouseMove"
                            PreviewMouseLeftButtonUp="ToolbarItem_PreviewMouseLeftButtonUp">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⋮" Foreground="#555555" VerticalAlignment="Center" Margin="2,0,4,0" Cursor="SizeAll" FontSize="14" FontWeight="Bold"/>
                            <Button Style="{StaticResource ToolbarButton}" Content="📄" Click="BtnNewQuery_Click" ToolTip="New Query Tab (Ctrl+N)"/>
                        </StackPanel>
                    </Border>

                    <Border Name="ToolbarInsertScript" Background="Transparent" Padding="2,0" Margin="0,0,4,0"
                            PreviewMouseLeftButtonDown="ToolbarItem_PreviewMouseLeftButtonDown"
                            PreviewMouseMove="ToolbarItem_PreviewMouseMove"
                            PreviewMouseLeftButtonUp="ToolbarItem_PreviewMouseLeftButtonUp">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⋮" Foreground="#555555" VerticalAlignment="Center" Margin="2,0,4,0" Cursor="SizeAll" FontSize="14" FontWeight="Bold"/>
                            <Button Style="{StaticResource ToolbarButton}" Content="📝 Insert Script" Click="BtnInsertScript_Click" ToolTip="Insert SQL Script Snippet"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <TextBlock Grid.Column="1" Text="MiniSSMS v1.1" Foreground="#555555" VerticalAlignment="Center" Margin="0,0,15,0" FontSize="11" FontStyle="Italic"/>
            </Grid>
        </Border>

        <!-- 2. Main Content Splitter (Left: Object Explorer, Right: Editor & Results) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="ObjectExplorerColumn" Width="260" MinWidth="180"/>
                <ColumnDefinition x:Name="ObjectExplorerSplitterColumn" Width="3"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Object Explorer -->
            <Grid x:Name="ObjectExplorerPanel" Grid.Column="0" Background="#1E1E1E">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="#252526" BorderBrush="#2D2D30" BorderThickness="0,0,1,1" Padding="12,8">
                    <TextBlock Text="Object Explorer" FontWeight="SemiBold" Foreground="#FFFFFF" FontSize="12"/>
                </Border>
                <ScrollViewer Grid.Row="1"
                              Background="#1E1E1E"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Auto"
                              CanContentScroll="False">
                    <ScrollViewer.Resources>
                        <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource StandardScrollBar}"/>
                    </ScrollViewer.Resources>
                    <TreeView x:Name="TreeObjectExplorer" BorderThickness="0"/>
                </ScrollViewer>
            </Grid>

            <!-- Vertical Grid Splitter -->
            <GridSplitter x:Name="ObjectExplorerSplitter" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#2D2D30" ShowsPreview="True"/>

            <!-- Right Panel: Multi-tab Query Editor & Results -->
            <TabControl x:Name="TabQueryControls" Grid.Column="2" BorderThickness="0" Background="#1E1E1E" Style="{StaticResource QueryTabsControl}" SelectionChanged="TabQueryControls_SelectionChanged">
                <!-- Tab items will be added programmatically here -->
            </TabControl>
        </Grid>

        <!-- 3. Status Bar -->
        <Border Grid.Row="2" Background="#007ACC" BorderBrush="#006699" BorderThickness="0,1,0,0" Padding="10,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock x:Name="TxtStatusServer" Text="Connected to Server" Foreground="White" FontSize="11" VerticalAlignment="Center"/>
                    <Separator Width="1" Margin="8,4" Background="#3399FF"/>
                    <TextBlock x:Name="TxtStatusDatabase" Text="master" Foreground="White" FontSize="11" VerticalAlignment="Center" FontWeight="SemiBold"/>
                </StackPanel>
                
                <TextBlock x:Name="TxtStatusTime" Grid.Column="2" Text="0 ms" Foreground="White" FontSize="11" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
