<Window x:Class="SSMS.ImportExcelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Data from Excel" 
        Height="800" Width="1100"
        MinHeight="650" MinWidth="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="#D4D4D4"
        FontFamily="Segoe UI, Arial"
        KeyDown="Window_KeyDown">
    
    <Window.Resources>
        <!-- Secondary Button Style -->
        <Style x:Key="DarkSecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="3" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3F3F46"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#555555"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#202020"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#202020"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#2B2B2B"/>
                                <Setter Property="Foreground" Value="#6A6A6A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="DarkPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DarkSecondaryButtonStyle}">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="BorderBrush" Value="#007ACC"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C97EA"/>
                    <Setter Property="BorderBrush" Value="#1C97EA"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#202020"/>
                    <Setter Property="BorderBrush" Value="#2B2B2B"/>
                    <Setter Property="Foreground" Value="#6A6A6A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Dark TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="SelectionBrush" Value="#007ACC"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom Dark ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid Name="MainGrid">
                            <Border x:Name="MainBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" SnapsToDevicePixels="True"/>
                            <ToggleButton Name="ToggleButton" Background="Transparent" BorderThickness="0" Foreground="{TemplateBinding Foreground}" Focusable="false" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent">
                                            <TextBlock Text="▼" FontSize="8" Foreground="#8A8A8A" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="8,3,25,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Name="DropDownBorder" Background="#252526" BorderBrush="#3C3C3C" BorderThickness="1" CornerRadius="3" Margin="0,2,0,0" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#555555"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderThickness="0" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2A2D2E"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="8" Background="#181818" BorderBrush="#3C3C3C" BorderThickness="1" CornerRadius="6">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.5" Color="#000000"/>
        </Border.Effect>

        <Grid Margin="18,14,18,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="*"/>    <!-- Main Form & Preview -->
                <RowDefinition Height="Auto"/> <!-- Progress / Status -->
                <RowDefinition Height="Auto"/> <!-- Actions -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,14" MouseDown="HeaderGrid_MouseDown" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Import Data from Excel" Foreground="#F2F2F2" FontSize="14" FontWeight="Bold"/>
                    <TextBlock Text="Pilih file Excel, tentukan nama tabel baru, lalu impor data ke SQL Server." Foreground="#8A8A8A" FontSize="11" TextWrapping="Wrap" Margin="0,3,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                    <Button Content="🗖" Width="26" Height="24" Background="Transparent" BorderThickness="0" Foreground="#A0A0A0" FontSize="11" Click="BtnMaximize_Click" Margin="0,0,4,0" ToolTip="Maximize / Restore">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="3">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="#2A2D2E"/>
                                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="✕" Width="26" Height="24" Background="Transparent" BorderThickness="0" Foreground="#A0A0A0" FontSize="11" Click="BtnClose_Click" ToolTip="Close">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="3">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="#C42B1C"/>
                                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Main Form & Preview Card -->
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Target Database -->
                    <RowDefinition Height="Auto"/> <!-- File Path + Browse + Reset -->
                    <RowDefinition Height="Auto"/> <!-- Sheet & Target Table Name -->
                    <RowDefinition Height="Auto"/> <!-- Max Rows & Options -->
                    <RowDefinition Height="*"/>    <!-- Data Preview Grid -->
                </Grid.RowDefinitions>

                <!-- Row 0: Target Database -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Database Tujuan:" Foreground="#CCCCCC" FontSize="12" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="TxtTargetDatabase" IsReadOnly="True" Background="#202020" Foreground="#007ACC" BorderBrush="#2B2B2B" Padding="8,4" FontSize="12" FontWeight="SemiBold"/>
                </Grid>

                <!-- Row 1: File Path + Browse + Reset -->
                <Grid Grid.Row="1" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="File Excel:" Foreground="#CCCCCC" FontSize="12" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="TxtFilePath" IsReadOnly="True" Background="#202020" Foreground="#F2F2F2" BorderBrush="#2B2B2B" Padding="8,4" FontSize="12"/>
                    <Button Grid.Column="2" x:Name="BtnBrowse" Content="📁 Browse..." Style="{StaticResource DarkSecondaryButtonStyle}" Margin="8,0,0,0" Click="BtnBrowse_Click"/>
                    <Button Grid.Column="3" x:Name="BtnReset" Content="↺ Reset" Style="{StaticResource DarkSecondaryButtonStyle}" Margin="6,0,0,0" Click="BtnReset_Click" IsEnabled="False"/>
                </Grid>

                <!-- Row 2: Sheet & Target Table -->
                <Grid Grid.Row="2" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Sheet Dropdown -->
                    <TextBlock Grid.Column="0" Text="Worksheet:" Foreground="#CCCCCC" FontSize="12" VerticalAlignment="Center"/>
                    <ComboBox Grid.Column="1" x:Name="CmbSheet" SelectionChanged="CmbSheet_SelectionChanged"/>

                    <!-- Target Table Name -->
                    <TextBlock Grid.Column="3" Text="Nama Tabel Baru:" Foreground="#CCCCCC" FontSize="12" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="4" x:Name="TxtTableName" TextChanged="TxtTableName_TextChanged"/>
                </Grid>

                <!-- Row 3: Max Preview Rows & Checkbox -->
                <Grid Grid.Row="3" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Max Sample Baris:" Foreground="#CCCCCC" FontSize="12" VerticalAlignment="Center" ToolTip="Batas jumlah baris yang dianalisa tipe datanya dan ditampilkan pada preview"/>
                    <TextBox Grid.Column="1" x:Name="TxtMaxRows" Text="100" HorizontalContentAlignment="Center" TextChanged="TxtMaxRows_TextChanged"/>
                    <CheckBox Grid.Column="3" x:Name="ChkFirstRowHeader" Content="Baris pertama adalah Nama Kolom (Header)" IsChecked="True" Foreground="#CCCCCC" FontSize="12" VerticalAlignment="Center" Click="ChkFirstRowHeader_Click"/>
                </Grid>

                <!-- Row 4: Data Preview -->
                <Grid Grid.Row="4" Margin="0,4,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" x:Name="TxtPreviewHeader" Text="Preview Data &amp; Kolom Terdeteksi (Max 100 Baris):" Foreground="#8A8A8A" FontSize="11" Margin="0,0,0,6"/>
                    <DataGrid Grid.Row="1" x:Name="GridPreview" AutoGenerateColumns="True" IsReadOnly="True" Background="#1E1E1E" Foreground="#CCCCCC" RowBackground="#181818" AlternatingRowBackground="#1E1E1E" BorderBrush="#2B2B2B" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#2B2B2B"/>
                </Grid>
            </Grid>

            <!-- Progress & Status Row -->
            <StackPanel Grid.Row="2" Margin="0,10,0,10">
                <ProgressBar x:Name="ProgressBarImport" Height="3" IsIndeterminate="False" Visibility="Collapsed" Foreground="#007ACC" Background="#252526" BorderThickness="0"/>
                <TextBlock x:Name="TxtStatus" Text="Silakan atur Max Preview Baris lalu pilih file Excel untuk memulai." Foreground="#8A8A8A" FontSize="11" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Buttons Row -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,4,0,0">
                <Button x:Name="BtnImport" Content="⚡ Import Data" Style="{StaticResource DarkPrimaryButtonStyle}" MinWidth="110" Height="30" Click="BtnImport_Click" IsEnabled="False" Margin="0,0,8,0"/>
                <Button x:Name="BtnCancel" Content="Batal" Style="{StaticResource DarkSecondaryButtonStyle}" MinWidth="90" Height="30" Click="BtnCancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
