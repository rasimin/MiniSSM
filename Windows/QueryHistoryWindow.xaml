<Window x:Class="SSMS.QueryHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Query Execution History"
        Width="1180" Height="730"
        MinWidth="900" MinHeight="580"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="#D4D4D4"
        FontFamily="Segoe UI, Arial"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- Secondary Button Style -->
        <Style x:Key="HistoryButton" TargetType="Button">
            <Setter Property="Height" Value="28"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3F3F46"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#555555"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#202020"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#202020"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2B2B2B"/>
                                <Setter Property="Foreground" Value="#6A6A6A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryHistoryButton" TargetType="Button" BasedOn="{StaticResource HistoryButton}">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="BorderBrush" Value="#007ACC"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C97EA"/>
                    <Setter Property="BorderBrush" Value="#1C97EA"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#202020"/>
                    <Setter Property="BorderBrush" Value="#2B2B2B"/>
                    <Setter Property="Foreground" Value="#6A6A6A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Dark TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="SelectionBrush" Value="#007ACC"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Multi-line Code & Log TextBox Style -->
        <Style x:Key="CodeBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#181818"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="BorderBrush" Value="#2B2B2B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="SelectionBrush" Value="#007ACC"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="True" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DatePickerTextBox Style (No Border, Transparent Background) -->
        <Style x:Key="DarkDatePickerTextBoxStyle" TargetType="DatePickerTextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="SelectionBrush" Value="#007ACC"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePickerTextBox">
                        <Grid Background="Transparent">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" VerticalAlignment="Center" Margin="6,0,0,0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DatePickerTextBox" BasedOn="{StaticResource DarkDatePickerTextBoxStyle}"/>

        <!-- Custom Dark DatePicker Styling -->
        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border x:Name="OuterBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="3" SnapsToDevicePixels="True">
                            <Grid x:Name="PART_Root" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="26"/>
                                </Grid.ColumnDefinitions>
                                
                                <DatePickerTextBox x:Name="PART_TextBox" 
                                                   Grid.Column="0"
                                                   Style="{StaticResource DarkDatePickerTextBoxStyle}"
                                                   Focusable="{TemplateBinding Focusable}" 
                                                   HorizontalContentAlignment="Stretch" 
                                                   VerticalContentAlignment="Center" 
                                                   Background="Transparent" 
                                                   Foreground="{TemplateBinding Foreground}"
                                                   BorderThickness="0" 
                                                   FontSize="12"/>

                                <Button x:Name="PART_Button" 
                                        Grid.Column="1" 
                                        Focusable="False" 
                                        Background="Transparent" 
                                        BorderThickness="0" 
                                        Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="btnBorder" Background="Transparent" CornerRadius="0,3,3,0">
                                                <Path x:Name="calIcon" 
                                                      Data="M 19 4 h-1 V 2 h-2 v 2 H 8 V 2 H 6 v 2 H 5 c-1.11 0-1.99.9-1.99 2 L 3 20 c 0 1.1.89 2 2 2 h 14 c 1.1 0 2-.9 2-2 V 6 c 0-1.1-.9-2-2-2 z m 0 16 H 5 V 10 h 14 v 10 z M 9 14 H 7 v-2 h 2 v 2 z m 4 0 h-2 v-2 h 2 v 2 z m 4 0 h-2 v-2 h 2 v 2 z M 9 18 H 7 v-2 h 2 v 2 z m 4 0 h-2 v-2 h 2 v 2 z m 4 0 h-2 v-2 h 2 v 2 z" 
                                                      Fill="#A0A0A0" 
                                                      Width="13" Height="13" 
                                                      Stretch="Uniform" 
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="btnBorder" Property="Background" Value="#3A3A3C"/>
                                                    <Setter TargetName="calIcon" Property="Fill" Value="#FFFFFF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>

                                <Popup x:Name="PART_Popup" 
                                       AllowsTransparency="True" 
                                       Placement="Bottom" 
                                       StaysOpen="False"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Dark Styles -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#2B2B2B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#181818"/>
            <Setter Property="AlternatingRowBackground" Value="#1E1E1E"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#2B2B2B"/>
            <Setter Property="VerticalGridLinesBrush" Value="#2B2B2B"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#202020"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#2B2B2B"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#181818"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A2D2E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Margin="8" Background="#181818" BorderBrush="#3C3C3C" BorderThickness="1" CornerRadius="6">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.5" Color="#000000"/>
        </Border.Effect>

        <Grid Margin="18,12,18,14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- 0. Window Title Bar (OUTSIDE Filter Box) -->
                <RowDefinition Height="Auto"/> <!-- 1. Filter Card Box -->
                <RowDefinition Height="Auto"/> <!-- 2. Grid Toolbar (Refresh & Open in New Query) -->
                <RowDefinition Height="*"/>    <!-- 3. DataGrid -->
                <RowDefinition Height="210"/>  <!-- 4. Query Details -->
            </Grid.RowDefinitions>

            <!-- 0. Top Window Title Bar (OUTSIDE filter box) -->
            <Grid Grid.Row="0" Margin="0,0,0,10" MouseDown="HeaderGrid_MouseDown" Background="Transparent">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Query Execution History" Foreground="#F2F2F2" FontSize="14" FontWeight="Bold"/>
                    <TextBlock x:Name="CountText" Text="Loading..." Foreground="#8A8A8A" FontSize="11" Margin="12,1,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Window Control Buttons (Top Right Corner Outside Card) -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="🗖" Width="26" Height="24" Background="Transparent" BorderThickness="0" Foreground="#A0A0A0" FontSize="11" Click="BtnMaximize_Click" Margin="0,0,4,0" ToolTip="Maximize / Restore">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="3">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="#2A2D2E"/>
                                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="✕" Width="26" Height="24" Background="Transparent" BorderThickness="0" Foreground="#A0A0A0" FontSize="11" Click="BtnClose_Click" ToolTip="Close">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="3">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="#C42B1C"/>
                                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- 1. Filter Card Box -->
            <Border Grid.Row="1" Background="#202020" BorderBrush="#2B2B2B" BorderThickness="1" CornerRadius="4" Padding="14,10" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="From" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="0,0,6,0" FontSize="12"/>
                    <DatePicker x:Name="FromDatePicker" Width="120"/>
                    
                    <TextBlock Text="To" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="10,0,6,0" FontSize="12"/>
                    <DatePicker x:Name="ToDatePicker" Width="120"/>
                    
                    <TextBlock Text="Database" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="10,0,6,0" FontSize="12"/>
                    <TextBox x:Name="DatabaseFilterTextBox" Width="140" KeyDown="FilterTextBox_KeyDown"/>
                    
                    <TextBlock Text="SQL contains" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="10,0,6,0" FontSize="12"/>
                    <TextBox x:Name="SqlFilterTextBox" Width="240" KeyDown="FilterTextBox_KeyDown"/>
                    
                    <Button Content="Apply" MinWidth="64" Style="{StaticResource PrimaryHistoryButton}" Click="ApplyFilterButton_Click" Margin="10,0,0,0"/>
                    <Button Content="Clear" MinWidth="64" Style="{StaticResource HistoryButton}" Click="ClearFilterButton_Click" Margin="6,0,0,0"/>
                </StackPanel>
            </Border>

            <!-- 2. Grid Toolbar (Refresh & Open in New Query Buttons directly ABOVE Grid) -->
            <Grid Grid.Row="2" Margin="0,0,0,8">
                <TextBlock Text="Execution Logs" Foreground="#8A8A8A" FontSize="11.5" VerticalAlignment="Center"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="🔄 Refresh" Style="{StaticResource HistoryButton}" Click="RefreshButton_Click" Margin="0,0,8,0"/>
                    <Button x:Name="OpenButton" Content="🚀 Open in New Query" MinWidth="140" IsEnabled="False" Style="{StaticResource PrimaryHistoryButton}" Click="OpenButton_Click"/>
                </StackPanel>
            </Grid>

            <!-- 3. DataGrid -->
            <DataGrid x:Name="HistoryGrid"
                      Grid.Row="3"
                      Margin="0,0,0,10"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      SelectionChanged="HistoryGrid_SelectionChanged"
                      MouseDoubleClick="HistoryGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Executed" Binding="{Binding ExecutedAtDisplay}" Width="145"/>
                    <DataGridTextColumn Header="Server" Binding="{Binding ServerName}" Width="160"/>
                    <DataGridTextColumn Header="Database" Binding="{Binding DatabaseDisplay}" Width="145"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding ExecutionStatus}" Width="80"/>
                    <DataGridTextColumn Header="Duration" Binding="{Binding DurationDisplay}" Width="90"/>
                    <DataGridTextColumn Header="Rows" Binding="{Binding RowsDisplay}" Width="105"/>
                    <DataGridTextColumn Header="Query" Binding="{Binding QueryPreview}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Loading Overlay -->
            <Border x:Name="LoadingOverlay" Grid.Row="3" Background="#181818" Visibility="Collapsed" Margin="0,0,0,10">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="3" Foreground="#007ACC" Background="#252526" BorderThickness="0" Margin="0,0,0,10"/>
                    <TextBlock Text="Loading history..." Foreground="#8A8A8A" HorizontalAlignment="Center" FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- 4. Expanded Query Details Section -->
            <Border Grid.Row="4" Background="#202020" BorderBrush="#2B2B2B" BorderThickness="1" CornerRadius="4" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="6*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="4*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="26"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Left: Executed Query -->
                    <TextBlock Grid.Column="0" Grid.Row="0" Text="Executed query" Foreground="#CCCCCC" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="12"/>
                    <Button x:Name="CopyQueryButton" Grid.Column="0" Grid.Row="0" Content="📋 Copy Query" Height="24" MinWidth="85" HorizontalAlignment="Right" VerticalAlignment="Center" IsEnabled="False" Style="{StaticResource HistoryButton}" Click="CopyQueryButton_Click"/>
                    
                    <TextBox x:Name="QueryTextBox" Grid.Column="0" Grid.Row="1" Margin="0,4,0,0" Style="{StaticResource CodeBoxStyle}" IsReadOnly="True" AcceptsReturn="True" AcceptsTab="True" TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"/>

                    <!-- Splitter -->
                    <GridSplitter Grid.Column="1" Grid.RowSpan="2" Width="6" Margin="4,0" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="#2B2B2B"/>

                    <!-- Right: Execution Message -->
                    <TextBlock Grid.Column="2" Grid.Row="0" Text="Execution message" Foreground="#CCCCCC" FontWeight="SemiBold" VerticalAlignment="Center" FontSize="12"/>
                    <TextBox x:Name="MessageTextBox" Grid.Column="2" Grid.Row="1" Margin="0,4,0,0" Style="{StaticResource CodeBoxStyle}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
