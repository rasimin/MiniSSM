<Window x:Class="SSMS.UnsafeExecutionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Konfirmasi Eksekusi Berbahaya"
        Width="580" MaxHeight="450" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Background="Transparent"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- Secondary Dialog Button -->
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="140"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Foreground" Value="#D0D0D0"/>
            <Setter Property="Background" Value="#252830"/>
            <Setter Property="BorderBrush" Value="#3F4350"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#323642"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#505565"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1C1E24"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Danger Dialog Button -->
        <Style x:Key="DangerDialogButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="#C42B1C"/>
            <Setter Property="BorderBrush" Value="#E03E2F"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E03E2F"/>
                    <Setter Property="BorderBrush" Value="#FF5555"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#A02015"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Margin="10" Background="#181A1F" BorderBrush="#4A2828" BorderThickness="1" CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="6" Opacity="0.75" Color="#000000"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="38"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="52"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#141113" BorderBrush="#2E1C1C" BorderThickness="0,0,0,1" CornerRadius="7,7,0,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" Margin="14,0,40,0" VerticalAlignment="Center">
                        <TextBlock Text="⚠️" FontSize="13" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Konfirmasi Eksekusi Berbahaya" VerticalAlignment="Center" Foreground="#F14C4C" FontSize="12" FontWeight="Bold"/>
                    </StackPanel>
                    <Button Width="28" Height="24" Margin="0,0,6,0" HorizontalAlignment="Right" VerticalAlignment="Center" Content="✕" Foreground="#888C94" Background="Transparent" BorderThickness="0" FontSize="11" Cursor="Hand" Click="BtnCancel_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="#FF5555"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="18,14,18,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="48"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Width="38" Height="38" HorizontalAlignment="Left" VerticalAlignment="Top" Background="#3D1818" BorderBrush="#7A2E2E" BorderThickness="1" CornerRadius="19" Margin="0,2,0,0">
                    <TextBlock Text="!" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FF5555" FontSize="20" FontWeight="Bold"/>
                </Border>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Potensi Bahaya Eksekusi (Safety Guardrail)" Foreground="#F0F0F0" FontSize="13" FontWeight="Bold"/>
                    <TextBlock Text="Query yang akan dieksekusi memuat perintah UPDATE atau DELETE TANPA klausa WHERE:" Foreground="#CCCCCC" FontSize="11" Margin="0,4,0,10" TextWrapping="Wrap"/>

                    <!-- Code Preview Box -->
                    <Border Background="#101216" BorderBrush="#2D313D" BorderThickness="1" CornerRadius="5" Padding="10" MaxHeight="160" Margin="0,0,0,12">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                            <TextBlock x:Name="TxtSnippet" FontFamily="Consolas" FontSize="12" Foreground="#CE9178" TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Border>

                    <TextBlock Text="Apakah Anda yakin ingin mengeksekusi query ini ke seluruh baris tabel?" Foreground="#E0E0E0" FontSize="12" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>

            <!-- Footer Buttons -->
            <Border Grid.Row="2" Background="#111317" BorderBrush="#2A2D35" BorderThickness="0,1,0,0" CornerRadius="0,0,7,7">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,14,0">
                    <Button Content="Batal (Rekomendasi)" Style="{StaticResource DialogButton}" IsDefault="True" Click="BtnCancel_Click"/>
                    <Button Content="Lanjutkan Eksekusi" Style="{StaticResource DangerDialogButton}" Click="BtnProceed_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
