<Window x:Class="SSMS.UnsafeExecutionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Konfirmasi Eksekusi Berbahaya"
        Width="560" MaxHeight="430" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Background="Transparent"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- Secondary Dialog Button -->
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="MinWidth" Value="130"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#3F3F46"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#555555"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#202020"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Danger Dialog Button -->
        <Style x:Key="DangerDialogButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="#C42B1C"/>
            <Setter Property="BorderBrush" Value="#C42B1C"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E03E2F"/>
                    <Setter Property="BorderBrush" Value="#E03E2F"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#A02015"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Margin="8" Background="#181818" BorderBrush="#3C3C3C" BorderThickness="1" CornerRadius="6" MouseLeftButtonDown="Header_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.5" Color="#000000"/>
        </Border.Effect>
        <Grid Margin="18,14,18,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- Top Row: Title & Close Button -->
            <Grid Grid.Row="0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⚠️" FontSize="13" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Konfirmasi Eksekusi Berbahaya" VerticalAlignment="Center" Foreground="#F14C4C" FontSize="13" FontWeight="Bold"/>
                </StackPanel>
                <Button Width="26" Height="26" HorizontalAlignment="Right" VerticalAlignment="Center" Content="✕" Foreground="#A0A0A0" Background="Transparent" BorderThickness="0" FontSize="11" Cursor="Hand" Click="BtnCancel_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="3">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="#C42B1C"/>
                                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Middle Row: Warning Content -->
            <Grid Grid.Row="1" Margin="0,14,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="42"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Width="32" Height="32" HorizontalAlignment="Left" VerticalAlignment="Top" Background="#3D1818" BorderBrush="#7A2E2E" BorderThickness="1" CornerRadius="16" Margin="0,2,0,0">
                    <TextBlock Text="!" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#F14C4C" FontSize="16" FontWeight="Bold"/>
                </Border>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Potensi Bahaya Eksekusi (Safety Guardrail)" Foreground="#F2F2F2" FontSize="13" FontWeight="Bold"/>
                    <TextBlock Text="Query yang akan dieksekusi memuat perintah UPDATE atau DELETE TANPA klausa WHERE:" Foreground="#CCCCCC" FontSize="11.5" Margin="0,4,0,10" TextWrapping="Wrap"/>

                    <!-- Code Preview Box -->
                    <Border Background="#202020" BorderBrush="#2B2B2B" BorderThickness="1" CornerRadius="4" Padding="10" MaxHeight="150" Margin="0,0,0,10">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                            <TextBlock x:Name="TxtSnippet" FontFamily="Consolas" FontSize="12" Foreground="#CE9178" TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Border>

                    <TextBlock Text="Apakah Anda yakin ingin mengeksekusi query ini ke seluruh baris tabel?" Foreground="#CCCCCC" FontSize="12" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>

            <!-- Bottom Row: Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Batal (Rekomendasi)" Style="{StaticResource DialogButton}" IsDefault="True" Click="BtnCancel_Click"/>
                <Button Content="Lanjutkan Eksekusi" Style="{StaticResource DangerDialogButton}" Click="BtnProceed_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
